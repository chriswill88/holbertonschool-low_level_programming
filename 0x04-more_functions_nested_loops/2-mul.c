#include "holberton.h"
#include <stdio.h>
/**
* mul - multiplying numbers
* @a: one number
* @b: a number to multiply with
* Return: results
*/
int mul(int a, int b)
{
	int results = a * b;

	return (results);
}
